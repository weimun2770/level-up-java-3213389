package com.linkedin.javacodechallenges;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

import java.util.Calendar;
import java.text.DateFormat;
import java.text.SimpleDateFormat;

import org.junit.Test;

/**
 * Unit test for simple App.
 */
public class AppTest 
{
    /**
     * Rigorous Test :-)
     */
    @Test
    public void shouldAnswerWithTrue()
    {
        // DateFormat dateFormat = new SimpleDateFormat("dd MMM yyyy");

        // Calendar dateAfter10Days = Calendar.getInstance();
        // dateAfter10Days.set(2023, Calendar.APRIL, 21);
        // assertEquals(dateFormat.format(dateAfter10Days.getTime()), App.afterDay(10));

        // Calendar dateAfter20Days = Calendar.getInstance();
        // dateAfter10Days.set(2023, Calendar.APRIL, 21);
    }
}
